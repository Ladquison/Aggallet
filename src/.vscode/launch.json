{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python Debugger",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/aggallet.py",
            "args": [
                "-o", "${workspaceFolder}/debug/output",
                "-s", "${workspaceFolder}/debug/spec",
                "-c"
            ],
            "console": "integratedTerminal"
        }
    ]
}